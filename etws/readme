2017年 04月 24日 星期一 14:54:41 CST
基于epoll的lt模型，能支持两万并发，web server的基本功能已经具备.
加上了线程池，貌似多线程处理静态请求还不如单线程。

2017年 04月 25日 星期二 11:19:42 CST
在epoll 的lt模型上又添加了epolloneshot 功能，防止多个线程共同读取同一个cfd。
当线程池的规模为1时，性能要好于单线程，但线程池越大，由于线程之间上下切换产生的开销，性能下降比较明显。
而且使用AB测试时，后10%请求处理的延迟问题也消失了，现在还搞不清楚是改了那里解决的问题，不过是好事情。
